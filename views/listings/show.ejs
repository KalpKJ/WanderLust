<!-- This file will display the details of a single listing.
It will display the title, description, price, location, and 
country of the listing. The data is passed to the view from the 
controller using the listing object. The listing object is an instance of the
Listing model, which contains the data. -->

<% layout('layouts/boilerplate') %>
<body>
    <h3>Listing Details :</h3>
    <ul>
        <li>Title: <%=listing.title%></li>
        <li>Title: <%=listing.description%></li>
        <li>Price: &#8377; <%=listing.price.toLocaleString("en-IN")%></li>
        <li>Location: <%=listing.location%></li>
        <li>Country: <%=listing.country%></li>
    </ul>
    <br>
    <a href = "/listings/<%=listing._id%>/edit">Edit this listing</a>
    <br><br/>

    <form method="POST" action="/listings/<%=listing._id%>?_method=DELETE">
        <button>Delete Listing</button>
    </form>
</body>
